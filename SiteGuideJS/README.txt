In order for the build to work, you need to run the command "npm i gulp-cli -g". This installs gulp globally.
To start the build in the terminal (bash), use the command "gulp".
Example of a line to include a block in index.html - @@include('html/testblock.html',{}).